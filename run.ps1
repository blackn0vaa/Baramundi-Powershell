# Setzen Sie den Benutzernamen, den wir in den folgenden Skripten verwenden werden
$Username = "Benutzername"

# Setzen Sie den Endpunktnamen, den wir in den folgenden Skripten verwenden werden
$EndpointName = "THINKPAD-10000"

# Dieses Skript sucht einen Benutzer nach seiner ID. Es ist derzeit auskommentiert, 
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\AD\searchUserByID.ps1 $Username

# Dieses Skript sucht einen Benutzer nach seinem Namen. Es ist derzeit auskommentiert, 
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\AD\searchUserByName.ps1 $Username

# Dieses Skript sucht einen Endpunkt nach seinem Namen. Es ist derzeit auskommentiert, 
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\Defense\searchEndpointByName.ps1 $EndpointName

# Dieses Skript holt alle Endpunkte. Es ist derzeit auskommentiert, 
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\Endpoints\getAllEndpoints.ps1

# Dieses Skript holt einen Endpunkt nach seinem Namen. Im Gegensatz zu den anderen Skripten 
# ist es nicht auskommentiert, also wird es ausgeführt, wenn Sie dieses Skript ausführen.
#.\Endpoints\getEndpointByName.ps1 $EndpointName

# Dieses Skript erstellt einen Endpunkt. Es ist derzeit auskommentiert,
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\Endpoints\createEndpoint.ps1 $EndpointName

# Dieses Skript löscht einen Endpunkt. Es ist derzeit auskommentiert,
# also wird es nicht ausgeführt, wenn Sie dieses Skript ausführen.
#.\Endpoints\deleteEndpoint.ps1 $EndpointName
